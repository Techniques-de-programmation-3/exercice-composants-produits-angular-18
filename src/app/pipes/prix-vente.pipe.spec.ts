import { PrixVentePipe } from './prix-vente.pipe';

describe('PrixVentePipe', () => {
  it('create an instance', () => {
    const pipe = new PrixVentePipe();
    expect(pipe).toBeTruthy();
  });
});
