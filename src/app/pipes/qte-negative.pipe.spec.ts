import { QteNegativePipe } from './qte-negative.pipe';

describe('QteNegativePipe', () => {
  it('create an instance', () => {
    const pipe = new QteNegativePipe();
    expect(pipe).toBeTruthy();
  });
});
