import { QteMinMaxPipe } from './qte-min-max.pipe';

describe('QteMinMaxPipe', () => {
  it('create an instance', () => {
    const pipe = new QteMinMaxPipe();
    expect(pipe).toBeTruthy();
  });
});
